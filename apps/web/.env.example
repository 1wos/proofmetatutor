# Cloud Run URL of the verifier API (no trailing slash). When unset, the UI
# falls back to an offline heuristic so it still demos.
# Live deployment (YOUR_GCP_PROJECT, asia-northeast3):
NEXT_PUBLIC_VERIFIER_API=<your Cloud Run URL>
